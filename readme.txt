USE:
The results inputted in the text field are saved in answers.txt, with the date and time being recorded as well.

REQUIRED PACKAGES (install using pip install or equivalent):
wxpython
datetime

RUN USING:
python3 main.py (or click the green triangle in PyCharm)